table:
  schema: public
  name: users
array_relationships:
  - name: bookmarks
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: bookmarks
  - name: ratings
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: ratings
  - name: tickets
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: tickets
